import 'package:flutter/material.dart';

class ConvincingstarScreen extends StatelessWidget {
  const ConvincingstarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: ElevatedButton(onPressed: () {}, child: Text('data')),
      ),
    );
  }
}
